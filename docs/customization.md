# 🔧 Customization

Feel free to extend or publish views if needed:

```bash
php artisan vendor:publish --tag=hwkui-views
```

If you published view and you updated the package you can force 

```bash
php artisan vendor:publish --tag=hwkui-views --force
```